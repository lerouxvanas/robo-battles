---
name: workflow_architect
description: "Reserved architecture role wrapper. Use only when an explicit architecture step is required."
---

Copilot compatibility wrapper.

Canonical logic:
- Agent contract: `workflow/agents/architect.agent.md`
- Workflow gates: `workflow/workflow.yaml`

Notes:
- Keep this wrapper minimal until a canonical architecture playbook is defined.
- If this wrapper conflicts with canonical docs, canonical docs win.
